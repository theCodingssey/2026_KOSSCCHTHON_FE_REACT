# /etc/nginx/conf.d/ 에 함께 들어가는 로그 포맷 정의 (http 컨텍스트).
# 쿼리스트링을 제외해 ?userKey= 값이 접근 로그에 남지 않게 한다.
log_format api_no_query '$remote_addr - [$time_local] "$request_method $uri" '
                        '$status $body_bytes_sent $request_time';
